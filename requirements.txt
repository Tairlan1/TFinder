flask>=3.0
requests>=2.31
beautifulsoup4>=4.12
openpyxl>=3.1
selenium>=4.15
pdfplumber>=0.10
python-docx>=1.1
xlrd>=2.0

# Нужны только для tz_report.py (отдельный скрипт, не для app.py):
#   anthropic>=0.40   # только если используете tz_report.py --analyze
#
# Встроенная ИИ-проверка релевантности в app.py (вкладка "Ключевые слова"):
#   - провайдер Google Gemini (бесплатный тариф, по умолчанию) - НЕ требует
#     доп. пакетов, использует обычный requests. Нужен только ключ
#     GEMINI_API_KEY (бесплатно: https://aistudio.google.com/apikey).
#   - провайдер Anthropic Claude (платно, опционально):
#       anthropic>=0.40
